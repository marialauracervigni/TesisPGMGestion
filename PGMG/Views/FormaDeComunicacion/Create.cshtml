@model PGMG.Models.FormaDeComunicacion

@{
    ViewBag.Title = "Cargar nueva forma de comunicación";
}

<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal"
            <span>&times;</span>
    </button>
</div>
<div class="modal-body centrar">
    <dl class="dl-horizontal">
        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })

            <div class="form-group">
                @Html.LabelFor(model => model.Detalle, htmlAttributes: new { @class = "control-label" })
                <br />
                @Html.EditorFor(model => model.Detalle, new { htmlAttributes = new { @class = "login" } })
                @Html.ValidationMessageFor(model => model.Detalle, "", new { @class = "text-danger" })
            </div>

            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-default" data-target="#modal-crear" data-toggle="modal" />
                <input type="button" value="Cancelar" class="btn btn-default" data-target="#modal-crear" data-toggle="modal" />
            </div>
        }
    </dl>
</div>


@section Scripts{
    @Scripts.Render("~/bundles/jqueryval")

    <script>
        function closeModal(data) {
            if (data.success) {
                $('#modal-crear').modal('hide');
                window.location.href = "/FormaDeComunicacion/index";
            }
        }
    </script>
}
