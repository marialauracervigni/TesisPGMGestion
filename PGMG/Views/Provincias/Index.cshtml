@model IEnumerable<PGMG.Models.Provincia>

@{
    ViewBag.Title = "Provincias";
}

<h3>Provincias</h3>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NombreP)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model)
{
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NombreP)
        </td>
        <td>
           @* @Html.ActionLink("Edit", "Edit", new { id=item.ProvinciaId }) |*@
            @*@Html.ActionLink("Details", "Details", new { id=item.ProvinciaId }) |*@
            @*@Html.ActionLink("Delete", "Delete", new { id=item.ProvinciaId })*@
        </td>
    </tr>
}

</table>
